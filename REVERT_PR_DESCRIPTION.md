# Revert "Feature/figure styles"

This pull request reverts the changes introduced in PR #1 ("Feature/figure styles").

What this PR does:

- Restores `about.html` to its previous version before PR #1.
- Restores `filming-veranda.html` to its previous version.
- Removes `styles.css` that was added by PR #1.
- Removes `assets/elder-veranda-interview.jpg` that was added by PR #1.

Reason for revert:

User requested to undo the changes made to the About page and associated assets.

```text
Reverts commit 5965d3a3531ffdf7aaec5331e4a0a0b169825848
```
